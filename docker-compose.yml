services:
  mongodb:
    image: mongo:7
    container_name: alma-jardin-mongodb
    restart: unless-stopped
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: alma-jardin
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
